# Agent direction

After every meaningful change, run the relevant checks and create a git commit before continuing. Keep commits small, descriptive, and focused. Never batch unrelated changes into one commit.

For the front-end, preserve the no-build-step vanilla HTML/CSS/JavaScript setup unless the project direction explicitly changes. Keep the interface keyboard-first, accessible, and playable without a network connection after the page loads.
